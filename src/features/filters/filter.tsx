import React, { FunctionComponent } from 'react';
import { FilterStyled } from './filter.styled';

const Filter: FunctionComponent<{}> = () => {
  return <FilterStyled>Here goes the filter if I have time</FilterStyled>;
};

export default Filter;
